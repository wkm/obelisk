package server

import (
	"testing"
)

func TestServer(t *testing.T) {
	t.Parallel()

	// tempdir, err := ioutil.TempDir(os.TempDir(), "obelisk")
	// if err != nil {
	// 	t.Fatal(err)
	// }

	// s := NewApp(Config{
	// 	StoreDir: tempdir,
	// })

	// s.Start()
}
