package server

import (
	"testing"
)

func TestServer(t *testing.T) {
	// tempdir, err := ioutil.TempDir(os.TempDir(), "obelisk")
	// if err != nil {
	// 	t.Fatal(err)
	// }

	// s := new(App)
	// s := NewApp(Config{
	// 	StoreDir: tempdir,
	// })

	// s.Start()
}
